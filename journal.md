Журнал выполнения практики

12.06.2026

Этап 1. Создание индекса файлов

Результат:

* Создана структура проекта.
* Настроено подключение к SQLite через модуль sqlite3.
* Создана база данных app.db.
* Создана таблица files для хранения информации о файлах.
* Реализована проверка существования указанной пользователем папки.
* Реализован запуск программы через аргументы командной строки.

Проблемы:

* Возникали ошибки при определении пути к базе данных.
* Требовалось проверить корректность создания таблицы при повторном запуске программы.

Следующий шаг:

* Реализовать рекурсивный обход папок и сохранение информации о найденных файлах.


13.06.2026

Этап 2. Сканирование файловой системы

Результат:

* Создан модуль scanner.py.
* Реализован рекурсивный обход каталогов с использованием собственной рекурсивной функции.
* Для каждого файла собираются:

  * относительный путь;
  * размер;
  * дата изменения;
  * расширение.
* Реализована фильтрация по расширению файлов.
* Добавлена возможность запуска программы с необязательным фильтром по расширению через аргументы командной строки.
* Реализовано сохранение найденных данных в SQLite.
* Добавлен вывод количества файлов и записей в базе данных.

Проблемы:

* Требовалось исключить из обработки служебные каталоги и файлы (.git, __pycache__, data, .gitkeep).
* Возникали ошибки импорта и отступов Python.

Следующий шаг:

* Добавить вычисление хэшей файлов для поиска дубликатов.


13.06.2026

Этап 3. Поиск дубликатов

Результат:

* Создан модуль hash_utils.py.
* Реализовано вычисление SHA-256 для каждого файла.
* В таблицу files добавлено поле file_hash.
* Реализовано сохранение хэшей в SQLite.
* Реализован поиск одинаковых файлов через SQL-запросы.
* Организован вывод списка дубликатов с указанием путей файлов.

Проблемы:

* Возникла ошибка SQL при добавлении нового столбца.
* Требовалось исправить INSERT-запрос после изменения структуры таблицы.

Следующий шаг:

* Реализовать сравнение исходной папки и резервной копии.


14.06.2026

Этап 4. Проверка резервной копии

Результат:

* Создан модуль backup_checker.py.
* Реализовано сравнение исходной папки и резервной копии.
* Определяются:

  * отсутствующие файлы;
  * лишние файлы;
  * изменённые файлы.
* Реализовано сравнение содержимого файлов по SHA-256.
* Создана таблица backup_checks для хранения результатов проверок.
* Реализован вывод количества выполненных проверок резервной копии.
* Добавлен итоговый отчёт о выполненной проверке.

Проблемы:

* Потребовалось доработать структуру базы данных для хранения истории запусков.
* Исправлены ошибки интеграции между backup_checker.py и db.py.

Следующий шаг:

* Подготовить проект к сдаче и проверить документацию.
